import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

// ------------------------------------------what happened in react changing complex state---------------------------------------
//described everything in the app.jsx, go there and have a look
//look at the last of vid 428 for more clarity
//this is all abt changing complex state in react, will add its practice in the next branch
//feels like uploading this part as a branch too, 4 more clarity ig :))
ReactDOM.render(
  <div>
    <App />
  </div>,
  document.getElementById("root")
);
