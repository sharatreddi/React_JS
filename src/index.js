import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

// ------------------------------------------what happened in react forms---------------------------------------
//this is abt diff between class components and functional components, how class components have bigger syntax
//how react developers got pissed off with all "this" statement usages in classes and else
//why they introduced hooks and they can be used in functional components and all
//compare both class component and functional component and know the diff, and also realise how irritating class components can be sometimes
//look at the last of vid 427 for more clarity

ReactDOM.render(<App />, document.getElementById("root"));
